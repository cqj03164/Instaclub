<p id="notice"><%= notice %></p>

<%= form_for(@insta3, url: choose_new_or_edit) do |f| %>

<%#= form_for(@insta3, url:confirm_insta3s_path) do |f| %>

  <% if @insta3.errors.any? %>
    <div id="error_explanation">
      <h2><%= @insta3.errors.count %>件のエラーがあります。</h2>

      <ul>
      <% @insta3.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :タイトル %><br>
    <%= f.text_field :title %>
  </div>
  
  <div class="field">
    <%= f.label :画像 %><br>
    <%= f.file_field :image %>
  </div>
  
  <div class="field">
    <%= f.label :コメント %><br>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to "投稿一覧画面にもどる", insta3s_path %>